DEV PLACEHOLDER — NOT THE TRANSCRIPT. THIS PIECE MUST NOT PUBLISH.

This file exists so the deploy preview of PR #191 can render the cover. It is
not the conversation, it is not evidence, and it discharges none of the promise
it stands in the place of.

WHAT BELONGS HERE. The cover is flagged `condensed_and_arranged`, which means
the editors omitted paragraphs and moved the most compelling part out of its
original order. The journal's published term is that "the full text as submitted
is always published at a permanent URL linked from the published page" — so this
address is where a reader checks the editors' cuts against the conversation as it
ran. Today that check is impossible: the only complete text is at DeepSeek's own
share link, on a platform the journal does not control and which can withdraw it.

WHAT MUST REPLACE IT. The editors' capture of the shared segment, whole, as it
ran — the work the desk scheduled for 2026-09-01 (Madison). The standing
instruction is that the cover does not ship to production before the real page
exists.

THE LATCH. `tests/as-submitted-no-scaffold.test.mjs` fails on this file for as
long as it says DEV PLACEHOLDER, so it cannot reach `main` by any path. Editing
this text to quiet the test, without replacing the file with the transcript,
would publish a page that reads as evidence and is not — which is worse than the
missing page it replaced.
